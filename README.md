# git_testing

```
npm i (if needed)
git init
touch .gitignore
attrib +h .gitgnore
### add files in this
git add .
git rm --cached -r .
-f for foce
git status
git commit -m "mess"
git remote add <name>(origin) url(github new repo without readme)
echo "# gti-testing ">>Readme.md
git add Readme.md
git commit -m "readme update"
git branch -M main
git push -u <remote name>(origin) <branch name>(main)


```
